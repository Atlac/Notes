Management plane - configuration and mangement of the devices
Control plane - information on how to move packet
Data plane - devices that route the traffic


log each action taken!!!
enforce the store of all the details
need a centralized solution 
use third party authentication so that each dont store the apssword but just the identity, and leave the authentication of all to the authentication authorities
NTP
to have a reliable accounting and auditing need the correct time and so have a reliable NTP (network time protocol)

SYSLOG
use standard mechanism to sore the log messages
for at least all the devices that are important
low level problem could not be sent but just stored

To access use encryption (DO IT)

DoS Protection
Router move packet that is what packet 
But if something is not related to routing they have to process, or if it's for the router itself, so that cpu is involved and maybe also a lot of the usage of the cpu is taken.
Routing table upadtes management of traffic service traffic ip options that require processing by routers.
Tru to protect the controll plane by shaping the pipeline or modelling the traffic.