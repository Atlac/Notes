every week can be exam on request
3 point for the excises - 27 points to the oral exam


Oblivious Transfer Tree based
since a path is logaritmic and bucket size is logaritmic is still polylog
ask for the path to a determieted leaf

after access a file add the file in the root and remove it from the old leaf so that i don't have to ask for the same path but also i need to hide the new potion so i locally memorize the position no globally since every element can be in the root.
The probability of a node being full is negligible.
Using the recursion of the request we remove the constraint of the memory of the client but with an increase of the operation time

Efficient OT Extension

